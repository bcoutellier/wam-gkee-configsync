apiVersion: configsync.gke.io/v1beta1
kind: RepoSync
metadata:
  name: repo-sync
  namespace: online-boutique
annotations:
  configsync.gke.io/cluster-name-selector: gke-cls-prd-2, gke-cls-prd-3
spec:
  sourceFormat: unstructured
  git:
    repo: https://github.com/bcoutellier/wam-gkee-configsync-apps
    branch: main
    revision: HEAD
    dir: "manifests/online-boutique"
    auth: none
